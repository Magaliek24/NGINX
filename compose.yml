# Environnement dev Nginx
services:
  monnginx: #nom du service
    build: ./front
    ports:
      - "8000:80"
    volumes:
      - ./front/app:/usr/share/nginx/html
    environment:
      - MYSQL-PASSWORD=${PASSWORD}




    #volumes:
     # - type: volume
      #  source: mes_datas
      #- type: bind
      #  source: ./front/app
      # target: /usr/share/nginx/html

#volumes:
 # mes_datas: